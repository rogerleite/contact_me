.group
  =f.label :subject
  =f.error_message_on :subject
  =f.text_field :subject, :class => :text_field
  %span.description Ex: a simple text

.group
  =f.label :email_to
  =f.error_message_on :email_to
  =f.text_field :email_to, :class => :text_field
  %span.description Ex: a simple text

.group.navform.wat-cf
  =f.submit pat(:save), :class => :button
  =f.submit pat(:cancel), :onclick => "window.location='#{url(:forms, :index)}';return false", :class => :button
